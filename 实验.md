# H2GCN npz 数据实验记录

## 实验目标

原论文复现实验依赖 signac 格式数据，但外部 Google Drive 数据链接目前不可稳定访问。因此这里使用作者后来提供的 `npz-datasets` 作为替代数据，目标不是严格复现论文表格，而是完成 method-level reproduction：

1. 使用官方 `syn-cora` npz 数据。
2. 在原 `h2gcn/run_experiments.py` 入口上读取 npz 数据。
3. 对比低/中/高同质性图上的 H2GCN-1 和 H2GCN-2 表现。
4. 观察模型在不同 homophily 水平下的趋势。

## 数据

使用：

```text
npz-datasets/syn-cora/
```

选择三个代表性同质性水平：

```text
h0.00-r1: 强异配
h0.50-r1: 中等同质性
h1.00-r1: 强同配
```

数据检查脚本：

```bash
/home/hmx42/miniconda3/envs/paper/bin/python "my code/feature-test.py"
```

已验证：

```text
h0.00-r1 actual edge homophily = 0.0000
h0.50-r1 actual edge homophily = 0.5115
h1.00-r1 actual edge homophily = 1.0000
```

## 数据入口

新增：

```text
h2gcn/datasets/npz.py
```

使原训练脚本可以使用：

```bash
cd h2gcn
python run_experiments.py H2GCN npz ...
```

npz 不包含论文原始 split，因此使用固定随机种子重新划分：

```text
split_seed = 15
train_per_class = 20
val_size = 500
test_size = 1000
```

对于 `syn-cora`，实际划分为：

```text
train = 100
val = 500
test = 890
```

## 模型设置

### H2GCN-1

```text
M64-R-T1-G-V-C1-D0.5-MO
```

含义：

- `M64`: 将输入特征映射到 64 维。
- `R`: ReLU。
- `T1`: 保存中间表示 tag 1。
- `G`: 图传播，使用一阶和二阶邻接。
- `V`: flatten 多 hop 表示。
- `C1`: 拼接当前表示和 tag 1。
- `D0.5`: dropout。
- `MO`: 输出层。

### H2GCN-2

```text
M64-R-T1-G-V-T2-G-V-C1-C2-D0.5-MO
```

相比 H2GCN-1，多了一轮图传播，并将两轮中间表示都拼接到最终分类器。

## 运行配置

为了先得到可讲的完整实验闭环，本次每个配置跑：

```text
epochs = 50
random_seed = 123
split_seed = 15
```

共 6 组实验：

```text
3 个 h × 2 个模型
```

结果目录：

```text
my code/full-rerun-checkpoints/
```

## 运行命令模板

```bash
cd h2gcn

/home/hmx42/miniconda3/envs/paper/bin/python run_experiments.py H2GCN npz \
  --dataset h0.50-r1 \
  --dataset_path ../npz-datasets/syn-cora \
  --network_setup M64-R-T1-G-V-C1-D0.5-MO \
  --epochs 50 \
  --random_seed 123 \
  --split_seed 15 \
  --run_id h0.50-r1_h2gcn1_e50 \
  --checkpoint_dir ../my\ code/full-rerun-checkpoints
```

## 实验结果

| Dataset | Homophily | Model | Epochs | Best Epoch | Train Acc | Val Acc | Test Acc |
|---|---:|---|---:|---:|---:|---:|---:|
| h0.00-r1 | 0.0000 | H2GCN-1 | 50 | 50 | 100.00% | 47.80% | 50.00% |
| h0.00-r1 | 0.0000 | H2GCN-2 | 50 | 13 | 60.00% | 28.00% | 29.44% |
| h0.50-r1 | 0.5115 | H2GCN-1 | 50 | 50 | 100.00% | 42.20% | 40.79% |
| h0.50-r1 | 0.5115 | H2GCN-2 | 50 | 50 | 81.00% | 36.80% | 34.16% |
| h1.00-r1 | 1.0000 | H2GCN-1 | 50 | 50 | 100.00% | 73.60% | 78.09% |
| h1.00-r1 | 1.0000 | H2GCN-2 | 50 | 50 | 100.00% | 100.00% | 99.89% |

日志文件：

```text
my code/full-rerun-logs/h0.00-r1_h2gcn1_e50.log
my code/full-rerun-logs/h0.00-r1_h2gcn2_e50.log
my code/full-rerun-logs/h0.50-r1_h2gcn1_e50.log
my code/full-rerun-logs/h0.50-r1_h2gcn2_e50.log
my code/full-rerun-logs/h1.00-r1_h2gcn1_e50.log
my code/full-rerun-logs/h1.00-r1_h2gcn2_e50.log
```

checkpoint 目录：

```text
my code/full-rerun-checkpoints/
```

## 当前结论

当前实验用于验证流程和趋势，不用于声称严格复现论文表格。严格复现仍需要原始 signac 数据和论文 split。

从这组 50 epoch 小实验可以先观察到：

1. `h=1.00` 的强同配图最容易，H2GCN-2 达到 `99.89%` test accuracy。
2. `h=0.00` 的强异配图上，H2GCN-1 比 H2GCN-2 更好；这说明更深的传播在当前设置下不一定总有利，尤其在小训练轮数和重新划分 split 下。
3. 这组实验主要说明 npz 数据入口和原训练脚本已经跑通，并能观察到不同 homophily 下模型表现差异。
4. 由于未做超参数搜索、未使用论文原始 signac split，也没有跑多随机种子，因此不能直接拿这些数值和论文表格比较。

下一步可以增加：

1. MLP baseline：只用节点特征，不用图结构。
2. 更多 `h` 值：例如 `0.10/0.20/.../0.90`。
3. 多个 repeat：`r1/r2/r3`。
4. 更长训练轮数和 early stopping。
5. 和 GCN / GraphSAGE / MixHop 的趋势对比。
